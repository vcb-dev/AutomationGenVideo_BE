name: CI

on:
  pull_request:
    branches: [main, truqhieu]
  push:
    branches: [main, truqhieu]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Lint & Build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Prisma generate cần DATABASE_URL/DIRECT_DATABASE_URL khi schema có env()
      - name: Prisma generate
        env:
          DATABASE_URL: postgresql://user:pass@localhost:5432/ci
          DIRECT_DATABASE_URL: postgresql://user:pass@localhost:5432/ci
        run: npx prisma generate

      - name: Build
        env:
          DATABASE_URL: postgresql://user:pass@localhost:5432/ci
          DIRECT_DATABASE_URL: postgresql://user:pass@localhost:5432/ci
        run: |
          rm -rf dist tsconfig.build.tsbuildinfo
          npm run build

      - name: Check dist outputs
        run: |
          test -f dist/src/main.js
          echo "Build OK — dist/src/main.js present"
